# Implementation Plan: <Semantic-Task-Name>

## 1. 架构与依赖分析 (Context)
*   **当前现状**：简述修改前相关模块的状态。
*   **影响面评估**：本次修改可能会影响到哪些模块或功能？（例如：如果修改了 A 函数，调用 A 的 B 组件是否需要同步修改？）

## 2. 修改范围 (Files to Modify)
> 明确列出本次计划新增、修改或删除的文件路径。

*   `path/to/file1.ts` (修改：调整 xx 逻辑)
*   `path/to/file2.py` (新增：提供 xx 接口)

## 3. 实施步骤 (Step-by-Step)
> 详细列出执行步骤，供 Backend/Frontend 逐步对照执行。

1.  **第一步**：(例如：在 x 文件中定义新的数据结构)。
2.  **第二步**：(例如：实现具体的数据处理逻辑)。
3.  **第三步**：(例如：在 UI 层接入新逻辑)。

## 4. 测试建议 (Testing Strategy)
*   开发人员在提交 Review 前，应如何进行本地自测？（例如：提供一些边界测试用例或建议运行的测试命令）。
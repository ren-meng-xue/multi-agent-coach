# QA Bank（面试题库）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户能上传自定义 Q&A 题库，面试时 AI Coach 优先从题库选题并对比参考答案给反馈。

**Architecture:** 新建 `user_qa_bank` 表存储题目，Markdown 解析服务按 category 覆盖写入；`InterviewSession` 新增 `use_qa_bank` 布尔字段，首轮 turn 时由前端传入并持久化到 session；Agent `_state_messages()` 在 session 开启 qa_bank 时将题目列表注入系统提示词。

**Tech Stack:** FastAPI + SQLAlchemy async + PostgreSQL + Alembic（后端），Next.js 14 App Router + TypeScript + Clerk Auth（前端），pytest-asyncio（测试）

---

## File Map

### 新建
| 文件 | 职责 |
|------|------|
| `backend/app/models/qa_bank.py` | UserQABank ORM 模型 |
| `backend/app/services/qa_bank.py` | Markdown 解析器 + CRUD |
| `backend/app/schemas/qa_bank.py` | Pydantic 响应 schema |
| `backend/app/api/v1/qa_bank.py` | 3 个 API 路由 |
| `backend/alembic/versions/XXXX_add_user_qa_bank.py` | 数据库迁移 |
| `backend/tests/test_qa_bank_parser.py` | 解析器单元测试 |
| `frontend/lib/qa-bank.ts` | 前端 API 客户端 |
| `frontend/app/settings/_components/qa-bank-card.tsx` | 设置页题库卡片 |

### 修改
| 文件 | 改动 |
|------|------|
| `backend/app/models/core.py` | InterviewSession 加 `use_qa_bank` 字段 |
| `backend/app/models/__init__.py` | register_models() 导入 qa_bank |
| `backend/app/main.py` | 挂载 qa_bank 路由 |
| `backend/app/schemas/interview.py` | TurnRequest 加 `use_qa_bank` |
| `backend/app/agents/interviewer/state.py` | InterviewState 加 `qa_bank_items` |
| `backend/app/agents/interviewer/nodes.py` | `_state_messages()` 注入题库 |
| `backend/app/services/interview_turn.py` | 加载题库 + 写 session 标志 |
| `frontend/app/settings/settings-view.tsx` | 引入 QABankCard |
| `frontend/lib/interview-chat.ts` | EnterInterviewRoomContext + turn 请求加 `use_qa_bank` |
| `frontend/app/coach/coach-dashboard.tsx` | 新增开关 UI |

---

## Task 1: DB Model + Migration

**Files:**
- Create: `backend/app/models/qa_bank.py`
- Modify: `backend/app/models/core.py` (line 69 — InterviewSession class)
- Modify: `backend/app/models/__init__.py`
- Create: `backend/alembic/versions/` (autogenerated)

- [ ] **Step 1: 创建 UserQABank ORM 模型**

```python
# backend/app/models/qa_bank.py
"""用户面试题库：按 category 存储用户自备的 Q&A 题目。"""
from datetime import datetime
from uuid import UUID

from sqlalchemy import JSON, CheckConstraint, DateTime, ForeignKey, String, Text, text
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import func

from app.db.base import Base


class UserQABank(Base):
    __tablename__ = "user_qa_bank"
    __table_args__ = (
        CheckConstraint(
            "category IN ('technical', 'hr', 'project')",
            name="ck_user_qa_bank_category",
        ),
    )

    id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        primary_key=True,
        server_default=text("gen_random_uuid()"),
    )
    user_id: Mapped[str] = mapped_column(
        String(64),
        ForeignKey("users.id", ondelete="CASCADE"),
        nullable=False,
        index=True,
    )
    category: Mapped[str] = mapped_column(String(20), nullable=False)
    question: Mapped[str] = mapped_column(Text, nullable=False)
    model_answer: Mapped[str] = mapped_column(Text, nullable=False)
    tags: Mapped[list[str] | None] = mapped_column(JSON)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )
```

- [ ] **Step 2: 向 InterviewSession 添加 `use_qa_bank` 字段**

在 `backend/app/models/core.py` 找到 `InterviewSession` 类，在 `report_json` 字段之后加一行：

```python
    use_qa_bank: Mapped[bool] = mapped_column(
        Boolean,
        nullable=False,
        server_default="false",
    )
```

（`Boolean` 已在文件顶部 import，无需新增 import）

- [ ] **Step 3: 注册新模型到 Alembic**

修改 `backend/app/models/__init__.py`：

```python
def register_models() -> None:
    from app.models import core  # noqa: F401
    from app.models import eval as _eval_models  # noqa: F401
    from app.models import qa_bank  # noqa: F401
```

- [ ] **Step 4: 生成 Alembic 迁移文件**

在 `backend/` 目录下运行：

```bash
cd backend
alembic revision --autogenerate -m "add_user_qa_bank"
```

Expected: 在 `backend/alembic/versions/` 生成新文件，内容包含 `create_table("user_qa_bank", ...)` 和 `add_column("interview_sessions", "use_qa_bank", ...)`。

- [ ] **Step 5: 检查生成的迁移文件**

确认迁移文件中：
- `upgrade()` 包含 `op.create_table("user_qa_bank", ...)` + `op.create_index(...)` + `op.add_column("interview_sessions", ...)`
- `downgrade()` 包含对应的 `op.drop_*`
- `down_revision` 指向 `"3c7aa213fcb6"`（eval indexes，当前 head）

- [ ] **Step 6: 应用迁移**

```bash
cd backend
alembic upgrade head
```

Expected: `Running upgrade 3c7aa213fcb6 -> XXXX, add_user_qa_bank`

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/qa_bank.py backend/app/models/core.py backend/app/models/__init__.py backend/alembic/versions/
git commit -m "feat(db): add user_qa_bank table and use_qa_bank flag on interview_sessions"
```

---

## Task 2: Markdown Parser（TDD）

**Files:**
- Create: `backend/tests/test_qa_bank_parser.py`
- Create: `backend/app/services/qa_bank.py`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_qa_bank_parser.py
"""unit tests for QA bank markdown parser — no DB required."""
import pytest
from app.services.qa_bank import parse_qa_markdown


def test_parse_happy_path():
    content = """\
## 技术题

### 题目 1
**问题：** 解释 RAG 的原理
**参考答案：** RAG 是检索增强生成
**标签：** AI, RAG, 检索增强

---

## HR题

### 题目 1
**问题：** 介绍一下你自己
**参考答案：** 我有 5 年经验
**标签：** 自我介绍

---

## 项目讲解

### 题目 1
**问题：** 介绍你的项目
**参考答案：** 这个项目的核心是
**标签：** AI Agent

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 3
    assert skipped == 0
    assert items[0].category == "technical"
    assert items[0].question == "解释 RAG 的原理"
    assert items[0].model_answer == "RAG 是检索增强生成"
    assert items[0].tags == ["AI", "RAG", "检索增强"]
    assert items[1].category == "hr"
    assert items[2].category == "project"


def test_skip_item_missing_answer():
    content = """\
## 技术题

### 题目 1
**问题：** 解释 RAG

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 0
    assert skipped == 1


def test_skip_item_missing_question():
    content = """\
## 技术题

### 题目 1
**参考答案：** 答案内容

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 0
    assert skipped == 1


def test_optional_tags():
    content = """\
## 技术题

### 题目 1
**问题：** 问题内容
**参考答案：** 答案内容

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 1
    assert items[0].tags is None


def test_partial_section_only():
    content = """\
## 技术题

### 题目 1
**问题：** Q1
**参考答案：** A1

---

### 题目 2
**问题：** Q2
**参考答案：** A2
**标签：** tag1, tag2

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 2
    assert all(i.category == "technical" for i in items)
    assert items[1].tags == ["tag1", "tag2"]


def test_unknown_section_ignored():
    content = """\
## 未知分类

### 题目 1
**问题：** Q
**参考答案：** A

---

## 技术题

### 题目 1
**问题：** Valid Q
**参考答案：** Valid A

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 1
    assert items[0].category == "technical"


def test_empty_content():
    items, skipped = parse_qa_markdown("")
    assert items == []
    assert skipped == 0


def test_mixed_valid_and_skipped():
    content = """\
## 技术题

### 题目 1
**问题：** Q1
**参考答案：** A1

---

### 题目 2
**问题：** Q2 no answer

---

### 题目 3
**问题：** Q3
**参考答案：** A3

---
"""
    items, skipped = parse_qa_markdown(content)
    assert len(items) == 2
    assert skipped == 1
```

- [ ] **Step 2: 运行测试，确认全部失败**

```bash
cd backend
pytest tests/test_qa_bank_parser.py -v
```

Expected: `FAILED` — `ImportError: cannot import name 'parse_qa_markdown'`

- [ ] **Step 3: 实现 parser 和 CRUD 服务**

```python
# backend/app/services/qa_bank.py
"""QA Bank 服务：Markdown 解析 + 数据库 CRUD。"""
from __future__ import annotations

import re
from dataclasses import dataclass, field

from sqlalchemy import delete, func, select
from sqlalchemy.ext.asyncio import AsyncSession

SECTION_MAP = {
    "技术题": "technical",
    "HR题": "hr",
    "项目讲解": "project",
}


@dataclass
class ParsedQAItem:
    category: str
    question: str
    model_answer: str
    tags: list[str] | None = field(default=None)


def parse_qa_markdown(content: str) -> tuple[list[ParsedQAItem], int]:
    """解析题库 Markdown，返回 (有效条目列表, 跳过数量)。

    字段匹配必须严格，缺少问题或参考答案的条目计入 skipped。
    """
    items: list[ParsedQAItem] = []
    skipped = 0
    current_category: str | None = None
    current_item: dict | None = None

    def _flush_item() -> None:
        nonlocal skipped, current_item
        if current_item is None:
            return
        if current_item.get("question") and current_item.get("model_answer"):
            items.append(
                ParsedQAItem(
                    category=current_item["category"],
                    question=current_item["question"],
                    model_answer=current_item["model_answer"],
                    tags=current_item.get("tags"),
                )
            )
        else:
            skipped += 1
        current_item = None

    for line in content.splitlines():
        stripped = line.strip()

        sec_match = re.match(r"^## (.+)$", stripped)
        if sec_match:
            _flush_item()
            current_category = SECTION_MAP.get(sec_match.group(1).strip())
            continue

        if current_category is None:
            continue

        if re.match(r"^### .+$", stripped):
            _flush_item()
            current_item = {"category": current_category}
            continue

        if current_item is None:
            continue

        q_match = re.match(r"^\*\*问题：\*\* (.+)$", stripped)
        if q_match:
            current_item["question"] = q_match.group(1).strip()
            continue

        a_match = re.match(r"^\*\*参考答案：\*\* (.+)$", stripped)
        if a_match:
            current_item["model_answer"] = a_match.group(1).strip()
            continue

        t_match = re.match(r"^\*\*标签：\*\* (.+)$", stripped)
        if t_match:
            raw = t_match.group(1).strip()
            current_item["tags"] = [t.strip() for t in raw.split(",") if t.strip()]
            continue

    _flush_item()
    return items, skipped


async def upsert_qa_bank(
    db: AsyncSession,
    *,
    user_id: str,
    items: list[ParsedQAItem],
) -> dict[str, int]:
    """按 category 覆盖写入题库条目，只更新 items 中出现过的 category。

    返回各 category 的导入数量。
    """
    from app.models.qa_bank import UserQABank

    categories_present = {item.category for item in items}
    for category in categories_present:
        await db.execute(
            delete(UserQABank).where(
                UserQABank.user_id == user_id,
                UserQABank.category == category,
            )
        )

    counts: dict[str, int] = {}
    for item in items:
        db.add(
            UserQABank(
                user_id=user_id,
                category=item.category,
                question=item.question,
                model_answer=item.model_answer,
                tags=item.tags,
            )
        )
        counts[item.category] = counts.get(item.category, 0) + 1

    await db.flush()
    return counts


async def get_qa_bank_summary(db: AsyncSession, *, user_id: str) -> dict[str, int]:
    """返回 {technical, hr, project} 各 category 的条目数。"""
    from app.models.qa_bank import UserQABank

    result = await db.execute(
        select(UserQABank.category, func.count().label("cnt"))
        .where(UserQABank.user_id == user_id)
        .group_by(UserQABank.category)
    )
    summary: dict[str, int] = {"technical": 0, "hr": 0, "project": 0}
    for row in result:
        summary[row.category] = row.cnt
    return summary


async def get_qa_bank_items(
    db: AsyncSession, *, user_id: str
) -> list:
    """返回用户所有题库条目（UserQABank 实例列表），按 category 和创建时间排序。"""
    from app.models.qa_bank import UserQABank

    result = await db.execute(
        select(UserQABank)
        .where(UserQABank.user_id == user_id)
        .order_by(UserQABank.category, UserQABank.created_at)
    )
    return list(result.scalars().all())
```

- [ ] **Step 4: 运行测试，确认全部通过**

```bash
cd backend
pytest tests/test_qa_bank_parser.py -v
```

Expected: `8 passed`

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/qa_bank.py backend/tests/test_qa_bank_parser.py
git commit -m "feat(service): add QA bank markdown parser with TDD coverage"
```

---

## Task 3: Backend API Routes

**Files:**
- Create: `backend/app/schemas/qa_bank.py`
- Create: `backend/app/api/v1/qa_bank.py`
- Modify: `backend/app/main.py`

- [x] **Step 1: 创建 Pydantic schemas**

```python
# backend/app/schemas/qa_bank.py
"""QA Bank 接口的请求/响应 schema。"""
from pydantic import BaseModel


class QABankSummary(BaseModel):
    technical: int
    hr: int
    project: int
    total: int


class QABankUploadResult(BaseModel):
    imported: dict[str, int]
    skipped: int
```

- [x] **Step 2: 创建 API 路由文件**

```python
# backend/app/api/v1/qa_bank.py
"""用户题库相关接口：模板下载、题库上传、摘要查询。"""
from fastapi import APIRouter, Depends, File, UploadFile
from fastapi.responses import Response as FastAPIResponse
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.auth import get_current_user_id
from app.core.exceptions import BadRequestException
from app.db.session import get_db
from app.schemas.qa_bank import QABankSummary, QABankUploadResult
from app.schemas.response import Response
from app.services.interview_turn import ensure_user_exists
from app.services.qa_bank import get_qa_bank_summary, parse_qa_markdown, upsert_qa_bank

_TEMPLATE = """\
## 技术题

### 题目 1
**问题：** 解释 RAG 的原理
**参考答案：** RAG 是检索增强生成，核心思路是...
**标签：** AI, RAG, 检索增强

---

### 题目 2
**问题：** 请在这里填写你的技术题
**参考答案：** 请填写参考答案
**标签：** 标签1, 标签2

---

## HR题

### 题目 1
**问题：** 介绍一下你自己
**参考答案：** 我有 X 年经验，专注于...
**标签：** 自我介绍

---

## 项目讲解

### 题目 1
**问题：** 介绍你最近一个项目
**参考答案：** 这个项目的核心是...
**标签：** 项目经验

---
"""

router = APIRouter(prefix="/user/qa-bank")


@router.get("/template")
async def download_template() -> FastAPIResponse:
    """下载空白 Markdown 题库模板文件。"""
    return FastAPIResponse(
        content=_TEMPLATE.encode("utf-8"),
        media_type="text/markdown; charset=utf-8",
        headers={"Content-Disposition": 'attachment; filename="面试题库模板.md"'},
    )


@router.post("/upload", response_model=Response[QABankUploadResult])
async def upload_qa_bank(
    file: UploadFile = File(...),
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
) -> Response[QABankUploadResult]:
    """上传填好的 Markdown 题库，解析后覆盖入库。"""
    filename = file.filename or ""
    if not filename.lower().endswith(".md"):
        raise BadRequestException("请上传 .md 格式的文件")

    raw = await file.read()
    try:
        content = raw.decode("utf-8")
    except UnicodeDecodeError:
        raise BadRequestException("文件编码不正确，请使用 UTF-8 编码保存后重新上传")

    if not content.strip():
        raise BadRequestException("文件内容为空")

    try:
        items, skipped = parse_qa_markdown(content)
    except Exception:
        raise BadRequestException("Markdown 格式错误，无法解析，请使用下载的模板填写")

    await ensure_user_exists(db, user_id=user_id)
    imported = await upsert_qa_bank(db, user_id=user_id, items=items)
    await db.commit()

    return Response.ok(
        data=QABankUploadResult(imported=imported, skipped=skipped),
        msg="上传成功",
    )


@router.get("/summary", response_model=Response[QABankSummary])
async def get_summary(
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
) -> Response[QABankSummary]:
    """返回各 category 题目数量。"""
    counts = await get_qa_bank_summary(db, user_id=user_id)
    return Response.ok(
        data=QABankSummary(
            technical=counts["technical"],
            hr=counts["hr"],
            project=counts["project"],
            total=sum(counts.values()),
        )
    )
```

- [x] **Step 3: 挂载路由到 main.py**

在 `backend/app/main.py` 顶部 imports 区域加一行：

```python
from app.api.v1 import qa_bank as qa_bank_v1
```

在末尾挂载路由的区域（紧跟 `app.include_router(coach_v1.router, ...)` 之后）加：

```python
app.include_router(qa_bank_v1.router, prefix="/api/v1", tags=["qa-bank"])
```

- [x] **Step 4: 手动验证路由注册**

```bash
cd backend
python -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'qa-bank' in r])"
```

Expected: `['/api/v1/user/qa-bank/template', '/api/v1/user/qa-bank/upload', '/api/v1/user/qa-bank/summary']`

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/qa_bank.py backend/app/api/v1/qa_bank.py backend/app/main.py
git commit -m "feat(api): add qa-bank routes: template download, upload, summary"
```

---

## Task 4: Interview Agent Integration

**Files:**
- Modify: `backend/app/schemas/interview.py`
- Modify: `backend/app/agents/interviewer/state.py`
- Modify: `backend/app/agents/interviewer/nodes.py`
- Modify: `backend/app/services/interview_turn.py`
- Modify: `backend/app/api/v1/interview.py`

- [x] **Step 1: 向 TurnRequest 加 `use_qa_bank` 字段**

在 `backend/app/schemas/interview.py` 的 `TurnRequest` 类中，`target_role` 字段之后加：

```python
    use_qa_bank: bool = False
```

- [x] **Step 2: 向 InterviewState 加 `qa_bank_items` 字段**

在 `backend/app/agents/interviewer/state.py` 的 `InterviewState` TypedDict 中，`report` 字段之前加：

```python
    qa_bank_items: list[dict] | None  # None 表示未启用题库；list 为空表示启用但无题目
```

- [x] **Step 3: 在 `_state_messages()` 中注入题库上下文**

在 `backend/app/agents/interviewer/nodes.py` 找到 `_state_messages()` 函数，在 `user_background` 注入之后、`return` 之前加：

```python
    qa_bank_items = state.get("qa_bank_items")
    if qa_bank_items:
        sections: dict[str, list[str]] = {"technical": [], "hr": [], "project": []}
        cat_labels = {"technical": "[技术题]", "hr": "[HR题]", "project": "[项目讲解]"}
        for item in qa_bank_items:
            cat = item.get("category", "technical")
            if cat in sections:
                sections[cat].append(
                    f"Q: {item['question']} / A（参考）: {item['model_answer']}"
                )
        block_lines = [
            "【用户已准备的题目库】",
            "以下题目请优先从中选取，覆盖整场面试：",
        ]
        for cat, label in cat_labels.items():
            if sections[cat]:
                block_lines.append(label)
                block_lines.extend(sections[cat])
        context_parts.append("\n".join(block_lines))
```

注意：`context_parts` 在函数中已有定义，直接 append 即可。确认 `return` 语句在此注入之后。

- [x] **Step 4: 向 `stream_interview_turn()` 加 `use_qa_bank` 参数**

在 `backend/app/services/interview_turn.py` 找到 `stream_interview_turn()` 函数，在参数列表末尾加：

```python
    use_qa_bank: bool = False,
```

完整函数签名变为：

```python
async def stream_interview_turn(
    message: str,
    *,
    user_id: str,
    db: AsyncSession,
    prepared_questions: list[dict[str, Any]] | None = None,
    jd_context: dict[str, Any] | None = None,
    target_role: str | None = None,
    use_qa_bank: bool = False,
) -> AsyncIterator[dict[str, Any]]:
```

- [x] **Step 5: 在 `stream_interview_turn()` 中设置 session 标志并加载题库**

在 `stream_interview_turn()` 内部，找到 `session, is_first_time = await get_or_create_active_session(...)` 之后，在 `history = await _load_session_messages(...)` 之前，加入：

```python
    # 首轮进入 opening 阶段时，根据前端传入值写入 session 标志
    if session.stage == "opening" and use_qa_bank and not session.use_qa_bank:
        session.use_qa_bank = True
        log.info("session_qa_bank_enabled", session_id=str(session.id), user_id=user_id)
```

然后在 `state = _build_state(...)` 之后加入题库加载逻辑：

```python
    # 若 session 启用了题库，加载题目并注入 state
    if session.use_qa_bank:
        from app.services.qa_bank import get_qa_bank_items
        qa_items = await get_qa_bank_items(db, user_id=user_id)
        if qa_items:
            state["qa_bank_items"] = [
                {
                    "category": item.category,
                    "question": item.question,
                    "model_answer": item.model_answer,
                }
                for item in qa_items
            ]
            log.info(
                "qa_bank_loaded",
                session_id=str(session.id),
                count=len(qa_items),
            )
        else:
            state["qa_bank_items"] = []
            log.info("qa_bank_enabled_but_empty", session_id=str(session.id))
```

- [x] **Step 6: 在 interview API 中透传 `use_qa_bank`**

在 `backend/app/api/v1/interview.py` 找到 `/turn` 端点内的 `stream_interview_turn(...)` 调用，加一个参数：

```python
            async for event in stream_interview_turn(
                req.message,
                user_id=user_id,
                db=db,
                prepared_questions=req.prepared_questions,
                jd_context=req.jd_context,
                target_role=req.target_role,
                use_qa_bank=req.use_qa_bank,   # ← 新增
```

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/interview.py \
        backend/app/agents/interviewer/state.py \
        backend/app/agents/interviewer/nodes.py \
        backend/app/services/interview_turn.py \
        backend/app/api/v1/interview.py
git commit -m "feat(agent): inject QA bank into interview state when session.use_qa_bank=true"
```

---

## Task 5: Frontend API Client

**Files:**
- Create: `frontend/lib/qa-bank.ts`

- [x] **Step 1: 创建 API 客户端**

```typescript
// frontend/lib/qa-bank.ts
/** 面试题库 API 封装。 */

export type QABankSummary = {
  technical: number;
  hr: number;
  project: number;
  total: number;
};

export type QABankUploadResult = {
  imported: { technical?: number; hr?: number; project?: number };
  skipped: number;
};

const getBaseUrl = () => {
  const baseUrl = process.env.NEXT_PUBLIC_API_URL;
  if (!baseUrl) throw new Error("缺少后端接口配置");
  return baseUrl.replace(/\/$/, "");
};

/** 获取题库各分类条目数。 */
export async function fetchQABankSummary({
  token,
}: {
  token: string;
}): Promise<QABankSummary> {
  const res = await fetch(`${getBaseUrl()}/api/v1/user/qa-bank/summary`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  if (!res.ok) throw new Error("获取题库摘要失败");
  const json = await res.json();
  return json.data as QABankSummary;
}

/** 触发浏览器下载题库 Markdown 模板。 */
export async function downloadQABankTemplate({
  token,
}: {
  token: string;
}): Promise<void> {
  const res = await fetch(`${getBaseUrl()}/api/v1/user/qa-bank/template`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  if (!res.ok) throw new Error("下载模板失败");
  const blob = await res.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = "面试题库模板.md";
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}

/** 上传填好的 Markdown 文件，解析并入库。 */
export async function uploadQABank({
  token,
  file,
}: {
  token: string;
  file: File;
}): Promise<QABankUploadResult> {
  const formData = new FormData();
  formData.append("file", file);
  const res = await fetch(`${getBaseUrl()}/api/v1/user/qa-bank/upload`, {
    method: "POST",
    headers: { Authorization: `Bearer ${token}` },
    body: formData,
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    throw new Error(body.msg || "上传失败");
  }
  const json = await res.json();
  return json.data as QABankUploadResult;
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/lib/qa-bank.ts
git commit -m "feat(frontend): add qa-bank API client (summary, template download, upload)"
```

---

## Task 6: Settings Page QA Bank Card

**Files:**
- Create: `frontend/app/settings/_components/qa-bank-card.tsx`
- Modify: `frontend/app/settings/settings-view.tsx`

- [x] **Step 1: 创建 QABankCard 组件**

```tsx
// frontend/app/settings/_components/qa-bank-card.tsx
"use client";

import React, { useRef, useState } from "react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Download, Upload } from "lucide-react";
import {
  fetchQABankSummary,
  downloadQABankTemplate,
  uploadQABank,
  type QABankSummary,
} from "@/lib/qa-bank";

type Props = {
  token: string;
  initialSummary: QABankSummary;
};

export function QABankCard({ token, initialSummary }: Props) {
  const [summary, setSummary] = useState<QABankSummary>(initialSummary);
  const [isUploading, setIsUploading] = useState(false);
  const [uploadMsg, setUploadMsg] = useState<string | null>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);

  const handleDownload = async () => {
    try {
      await downloadQABankTemplate({ token });
    } catch {
      setUploadMsg("下载失败，请稍后重试");
    }
  };

  const handleUploadClick = () => {
    fileInputRef.current?.click();
  };

  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file) return;
    e.target.value = "";

    setIsUploading(true);
    setUploadMsg(null);
    try {
      const result = await uploadQABank({ token, file });
      const total = Object.values(result.imported).reduce((a, b) => a + b, 0);
      const skipText = result.skipped > 0 ? `，跳过 ${result.skipped} 条` : "";
      setUploadMsg(`上传成功，导入 ${total} 条题目${skipText}`);
      const newSummary = await fetchQABankSummary({ token });
      setSummary(newSummary);
    } catch (err) {
      setUploadMsg(err instanceof Error ? err.message : "上传失败");
    } finally {
      setIsUploading(false);
    }
  };

  const summaryText =
    summary.total === 0
      ? "暂无题目"
      : `技术题 ${summary.technical} 条 | HR题 ${summary.hr} 条 | 项目讲解 ${summary.project} 条`;

  return (
    <Card className="p-6 border-[#e8e7e2] bg-white shadow-sm">
      <div className="flex items-start justify-between mb-3">
        <div>
          <h3 className="font-bold text-[#171717] text-base">面试题库</h3>
          <p className="text-xs text-[#8a8a8a] mt-0.5">提前准备题目，AI Coach 直接考你</p>
        </div>
        <div className="flex gap-2">
          <Button
            variant="outline"
            size="sm"
            onClick={handleDownload}
            className="gap-1.5 text-xs rounded-lg border-[#e8e7e2] text-[#525252]"
          >
            <Download className="size-3.5" /> 下载模板
          </Button>
          <Button
            size="sm"
            onClick={handleUploadClick}
            disabled={isUploading}
            className="bg-[#4f46e5] hover:bg-[#4338ca] text-white gap-1.5 text-xs rounded-lg"
          >
            <Upload className="size-3.5" />
            {isUploading ? "上传中..." : "上传题库"}
          </Button>
          <input
            type="file"
            ref={fileInputRef}
            onChange={handleFileChange}
            accept=".md"
            className="hidden"
          />
        </div>
      </div>
      <p className="text-sm text-[#525252] font-medium">{summaryText}</p>
      {uploadMsg && (
        <p className="text-xs mt-2 text-[#059669]">{uploadMsg}</p>
      )}
    </Card>
  );
}
```

- [x] **Step 2: 在 settings-view.tsx 中引入 QABankCard**

在 `frontend/app/settings/settings-view.tsx` 顶部 imports 加：

```typescript
import { QABankCard } from "./_components/qa-bank-card";
import { fetchQABankSummary, type QABankSummary } from "@/lib/qa-bank";
```

在 `stories` state 之后加 QA bank state：

```typescript
  const [qaBankSummary, setQABankSummary] = useState<QABankSummary>({
    technical: 0, hr: 0, project: 0, total: 0,
  });
```

在 `loadData()` 内 `Promise.all` 中加载 summary：

```typescript
        const [userProfile, userStories, qaSummary] = await Promise.all([
          fetchUserProfile({ token }),
          fetchUserStories({ token }),
          fetchQABankSummary({ token }),
        ]);

        setProfile(userProfile);
        setStories(userStories);
        setQABankSummary(qaSummary);
```

在右侧故事库区块的最下方（`stories.map(...)` 或空状态 div 之后），加一个 `<div>` 包裹 QABankCard，与故事库卡片保持相同容器节奏：

```tsx
          {/* 面试题库 */}
          <QABankCard
            token={token ?? ""}
            initialSummary={qaBankSummary}
          />
```

注意：`token` 从 `await getToken()` 取得后需要存在 state 中供子组件使用，或直接用 `getToken` 传入并在子组件内调用。当前代码在 `loadData()` 内取 token 仅用于加载，不存 state。最简做法：将 `token` 存入 state：

```typescript
  const [authToken, setAuthToken] = useState<string>("");
```

在 `loadData()` 中：

```typescript
        const token = await getToken();
        if (!token) return;
        setAuthToken(token);
```

然后传给 `QABankCard`：

```tsx
          <QABankCard token={authToken} initialSummary={qaBankSummary} />
```

- [ ] **Step 3: Commit**

```bash
git add frontend/app/settings/_components/qa-bank-card.tsx \
        frontend/app/settings/settings-view.tsx
git commit -m "feat(settings): add QA bank card with download template + upload"
```

---

## Task 7: Coach Dashboard Toggle

**Files:**
- Modify: `frontend/lib/interview-chat.ts`
- Modify: `frontend/app/coach/coach-dashboard.tsx`

- [x] **Step 1: 向 `EnterInterviewRoomContext` 加 `use_qa_bank` 字段**

在 `frontend/lib/interview-chat.ts` 找到 `EnterInterviewRoomContext` 类型定义，加一个可选字段：

```typescript
export type EnterInterviewRoomContext = {
  target_role?: string;
  user_background?: string;
  jd_text?: string;
  jd_url?: string;
  use_qa_bank?: boolean;   // ← 新增
};
```

- [x] **Step 2: 向 `StreamInterviewChatOptions` 和 turn 请求加 `useQABank`**

在 `frontend/lib/interview-chat.ts` 找到 `StreamInterviewChatOptions` 类型，加：

```typescript
  useQABank?: boolean;
```

在 `streamInterviewChat` 函数的解构参数中加 `useQABank`，并在 `body: JSON.stringify({...})` 中加：

```typescript
      use_qa_bank: useQABank ?? false,
```

完整 body 变为：

```typescript
    body: JSON.stringify({
      message,
      prepared_questions: preparedQuestions,
      jd_context: jdContext,
      target_role: targetRole,
      use_qa_bank: useQABank ?? false,
    }),
```

- [x] **Step 3: 在 interview-chat.tsx 读取并传递 use_qa_bank（首轮）**

在 `frontend/app/interview/_components/interview-chat.tsx` 中，找到 `globalInitialContextCache` 的类型声明（或第一次 `streamInterviewChat` 调用处），在首轮调用（`message: "__START__"`）加 `useQABank`：

找到大约 line 504-509 的调用：

```typescript
      await streamInterviewChat({
        token,
        message: "__START__",
        preparedQuestions: prepareData.questions,
        jdContext: prepareData.jdContext,
        targetRole: prepareData.direction,
        useQABank: initialContextRef.current?.use_qa_bank ?? false,   // ← 新增
        signal: abortController.signal,
        ...
      });
```

- [x] **Step 4: 在 coach-dashboard.tsx 加 `useQABank` state 和 summary 加载**

在 `frontend/app/coach/coach-dashboard.tsx` 顶部 imports 加：

```typescript
import { fetchQABankSummary, type QABankSummary } from "@/lib/qa-bank";
```

在组件内已有 state 之后加：

```typescript
  const [useQABank, setUseQABank] = useState(false);
  const [qaBankTotal, setQABankTotal] = useState(0);
```

在组件的 `useEffect`（加载 context 或 coach 数据的地方），加载 QA bank 摘要。找到已有 token 的 useEffect，加：

```typescript
      const summary = await fetchQABankSummary({ token });
      setQABankTotal(summary.total);
```

在渲染 `enterInterviewRoom` 按钮之前，当 `qaBankTotal > 0` 时展示开关：

```tsx
          {qaBankTotal > 0 && (
            <div className="flex items-center justify-between rounded-xl border border-[#e8e7e2] bg-white px-4 py-3 mb-3">
              <div>
                <p className="text-sm font-medium text-[#171717]">本次使用我的题库</p>
                <p className="text-xs text-[#8a8a8a]">
                  开启后 AI 将从你的 {qaBankTotal} 道准备题中选题
                </p>
              </div>
              <button
                onClick={() => setUseQABank((v) => !v)}
                className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${
                  useQABank ? "bg-[#4f46e5]" : "bg-[#e8e7e2]"
                }`}
                aria-label="切换题库模式"
              >
                <span
                  className={`inline-block h-4 w-4 transform rounded-full bg-white shadow transition-transform ${
                    useQABank ? "translate-x-6" : "translate-x-1"
                  }`}
                />
              </button>
            </div>
          )}
```

在调用 `enterInterviewRoom(...)` 的地方，把 `useQABank` 加入 context：

```typescript
        await enterInterviewRoom({
          getToken,
          router,
          context: {
            target_role: ...,
            user_background: ...,
            use_qa_bank: useQABank,   // ← 新增
          },
        });
```

- [ ] **Step 5: Commit**

```bash
git add frontend/lib/interview-chat.ts \
        frontend/app/interview/_components/interview-chat.tsx \
        frontend/app/coach/coach-dashboard.tsx
git commit -m "feat(coach): add use_qa_bank toggle and wire through to interview turn request"
```

---

## Self-Review

**Spec coverage check:**

| Spec 需求 | 对应 Task |
|-----------|-----------|
| 下载 Markdown 模板 | Task 3 `/template` 路由 + Task 6 UI |
| 上传填好的模板解析入库 | Task 2 parser + Task 3 `/upload` 路由 + Task 6 UI |
| 按 category 覆盖（不影响其他 category）| Task 2 `upsert_qa_bank()` |
| 缺失必填字段的条目跳过并计数 | Task 2 parser `skipped` 返回值 |
| 设置页展示题库条目数量摘要 | Task 3 `/summary` 路由 + Task 6 QABankCard |
| 面试开始前的开关（有题库才展示）| Task 7 toggle |
| `use_qa_bank` 持久化到 session | Task 4 `stream_interview_turn()` |
| Agent 从题库选题（注入提示词）| Task 4 `_state_messages()` |
| 每轮对比参考答案给反馈 | Task 4（通过提示词注入隐式实现，LLM 可见参考答案） |
| 题库为空但开关开启 → 降级正常模式 | Task 4 `qa_bank_items = []` 时不注入题库块 |
| 上传非 .md 文件返回 400 | Task 3 `BadRequestException` |
| `InterviewSession.use_qa_bank` 字段 | Task 1 DB model |
| 新表 `user_qa_bank` | Task 1 DB model |

**Placeholder scan:** 无占位符，所有步骤均有完整代码。

**Type consistency check:**
- `ParsedQAItem.category` ↔ `UserQABank.category` ↔ `QABankSummary` 字段名：均为 `technical` / `hr` / `project` ✓
- `parse_qa_markdown()` 返回 `tuple[list[ParsedQAItem], int]`，Task 2 测试和 Task 3 调用均匹配 ✓
- `upsert_qa_bank()` 返回 `dict[str, int]`，Task 3 `QABankUploadResult.imported` 类型 `dict[str, int]` ✓
- `get_qa_bank_items()` 返回 `list[UserQABank]`，Task 4 通过 `.category`, `.question`, `.model_answer` 访问 ✓
- `QABankSummary.total`，Task 5 / Task 6 / Task 7 均访问 `.total` ✓
- `EnterInterviewRoomContext.use_qa_bank?: boolean`，Task 7 传入和 interview-chat.tsx 读取均用 `use_qa_bank` ✓
- `StreamInterviewChatOptions.useQABank?: boolean`（camelCase），转成 `use_qa_bank` snake_case 写入请求体 ✓

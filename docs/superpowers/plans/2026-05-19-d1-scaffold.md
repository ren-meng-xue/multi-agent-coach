# D1 - 脚手架 + 数据层 + Clerk 接入（10h）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 `multi-agent-coach/` 全栈仓库初始化、Docker 基础设施、7 张表数据层（user_id 为 Clerk VARCHAR）、FastAPI 入口、Clerk 注册 + env keys；EOD `./dev.sh` 能起服务，`curl /health` 返回 ok。

**Architecture:** 仓库根目录下分 `backend/`（Python uv）+ `frontend/`（Next.js pnpm）+ `docker-compose.yml`（postgres-pgvector + redis）+ `scripts/` + `docs/`。后端用 SQLAlchemy 2.x async + Alembic + pydantic-settings + structlog。`users.id` 改为 `VARCHAR(64)` 存储 Clerk user_id（格式 `user_2abcDEF...`）。鉴权：前端 `@clerk/nextjs` 中间件 + 后端 `pyjwt[crypto]` 校验 Clerk JWT。

**Tech Stack:** Python 3.12 / uv / FastAPI / SQLAlchemy 2 async / asyncpg / Alembic / pgvector / structlog / pydantic-settings / **pyjwt[crypto]** / **Clerk** / Next.js 16 / Tailwind 4 / shadcn/ui / **@clerk/nextjs** / Docker Compose / pnpm。

**输入：** D0 启动日清单已勾完（Clerk 注册 + 3 个 keys + API Keys + GitHub repo + 本机依赖）

**输出：** D1 EOD commit `feat: 工程脚手架 + 数据层 + Clerk env`，验收清单 §15 全过。

---

## Task 1: 初始化仓库 + 顶层骨架

**Files:**
- Create: `/Users/xuebao/learn/AI项目/multi-agent-coach/.gitignore`
- Create: `/Users/xuebao/learn/AI项目/multi-agent-coach/README.md`
- Create: `/Users/xuebao/learn/AI项目/multi-agent-coach/CLAUDE.md`
- Create: `/Users/xuebao/learn/AI项目/multi-agent-coach/AGENTS.md`

- [ ] **Step 1: 确认 cwd 是仓库根 + git init**

```bash
cd /Users/xuebao/learn/AI项目/multi-agent-coach
git init -b main
```
Expected: `Initialized empty Git repository in .../multi-agent-coach/.git/`

- [ ] **Step 2: 写 `.gitignore`**

```gitignore
# Python
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
.turbo/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker volumes
postgres-data/
redis-data/

# Logs / coverage
*.log
coverage/
htmlcov/
.coverage

# Build artifacts
dist/
build/
```

- [ ] **Step 3: 写最简 `README.md`（D7 会重写）**

```markdown
# Multi Agent Coach

AI Agent 工程师面试陪练系统。多 Agent + 分级长期记忆 + Reflexion 自反思。

> 🚧 D1: 工程脚手架阶段，详见 `docs/superpowers/plans/`。
```

- [ ] **Step 4: 复制全局规范到 `CLAUDE.md` 和 `AGENTS.md`（占位骨架）**

`CLAUDE.md` 内容：

```markdown
# Multi Agent Coach · Claude Code 主控规范

参考 `docs/plans/m1-foundation.md` §3.8 工程规范条款，本仓库强制执行：

1. 永远不要 `git add -A` / `git add .`，按文件名添加
2. 所有新代码先写失败测试（TDD）
3. 配置走 `pydantic-settings`，禁止硬编码 Key
4. 数据库迁移走 Alembic，禁止直接改表
5. 异步代码用 SQLAlchemy 2.x async + asyncpg
6. 前端组件优先用 shadcn/ui
7. 日志走 `structlog`，禁止 `print`
8. LLM 调用必须有 retry 装饰器 + 失败日志
9. Commit 走 conventional commits（feat/fix/test/refactor/docs/chore/build）
10. 每完成一个可独立验证单元立即 commit

## 执行计划入口

- 战略：`docs/plans/7-day-ship-plan.md`
- 执行：`docs/superpowers/plans/2026-05-19-00-index.md`
```

`AGENTS.md`：内容与 `CLAUDE.md` 完全一致（Codex 同步用）。

- [ ] **Step 5: 首次 commit**

```bash
git add .gitignore README.md CLAUDE.md AGENTS.md
git commit -m "chore: init repo with global conventions"
```
Expected: `1 file changed` ... commit hash 输出。

---

## Task 2: 顶层目录结构 + 占位文件

**Files:**
- Create: `backend/.gitkeep`
- Create: `frontend/.gitkeep`
- Create: `scripts/.gitkeep`
- Create: `docs/.gitkeep`（实际已存在内容，跳过）

- [ ] **Step 1: 建立目录骨架**

```bash
mkdir -p backend/app/{api/v1,services/{agents,rag,memory,reflexion},models,schemas,core,workers,db}
mkdir -p backend/tests/{unit,integration}
mkdir -p backend/alembic/versions
mkdir -p frontend
mkdir -p scripts
touch backend/.gitkeep backend/tests/.gitkeep backend/alembic/versions/.gitkeep frontend/.gitkeep scripts/.gitkeep
```

- [ ] **Step 2: 验证目录结构**

```bash
find backend -type d | sort
```
Expected: 列出 `backend/app/api/v1` `backend/app/services/agents` ... `backend/tests/integration`

- [ ] **Step 3: commit**

```bash
git add backend/.gitkeep backend/tests/.gitkeep backend/alembic/versions/.gitkeep frontend/.gitkeep scripts/.gitkeep
git commit -m "chore: scaffold backend/frontend/scripts directories"
```

---

## Task 3: backend 依赖管理（uv + pyproject.toml）

**Files:**
- Create: `backend/pyproject.toml`
- Create: `backend/.python-version`

- [ ] **Step 1: 写 `backend/pyproject.toml`**

```toml
[project]
name = "multi-agent-coach-backend"
version = "0.1.0"
description = "Multi Agent Coach backend"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pgvector>=0.3",
    "pydantic>=2.10",
    "pydantic-settings>=2.7",
    "structlog>=24.4",
    "celery[redis]>=5.4",
    "redis>=5.2",
    "pyjwt[crypto]>=2.9",
    "openai>=1.55",
    "langgraph>=0.2.50",
    "langchain-core>=0.3",
    "langchain-openai>=0.2",
    "sse-starlette>=2.1",
    "firecrawl-py>=1.6",
    "tenacity>=9.0",
    "httpx>=0.28",
    "python-multipart>=0.0.20",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "httpx>=0.28",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "C4", "SIM"]
ignore = ["E501"]
```

- [ ] **Step 2: 写 `.python-version`**

```
3.12
```

- [ ] **Step 3: uv sync 安装依赖**

```bash
cd backend && uv sync
```
Expected: `Resolved N packages` + `Installed N packages`，生成 `uv.lock` 和 `.venv/`。

- [ ] **Step 4: 验证 Python 版本**

```bash
cd backend && uv run python -c "import sys; print(sys.version)"
```
Expected: `3.12.x ...`

- [ ] **Step 5: commit**

```bash
git add backend/pyproject.toml backend/.python-version backend/uv.lock
git commit -m "build: setup backend with uv + python 3.12"
```

---

## Task 4: backend 配置层（pydantic-settings）

**Files:**
- Create: `backend/app/__init__.py`
- Create: `backend/app/core/__init__.py`
- Create: `backend/app/core/config.py`
- Create: `backend/.env.example`
- Test: `backend/tests/unit/test_config.py`

- [ ] **Step 1: 写失败测试 `tests/unit/test_config.py`**

```python
from app.core.config import Settings


def test_settings_loads_required_env(monkeypatch):
    monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@h:5432/db")
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("OPENAI_API_KEY", "sk-test")
    monkeypatch.setenv("FIRECRAWL_API_KEY", "fc-test")
    monkeypatch.setenv("CLERK_JWT_KEY", "-----BEGIN PUBLIC KEY-----\ntest\n-----END PUBLIC KEY-----")
    s = Settings()
    assert s.database_url == "postgresql+asyncpg://u:p@h:5432/db"
    assert s.redis_url == "redis://localhost:6379/0"
    assert s.openai_api_key.get_secret_value() == "sk-test"
    assert s.firecrawl_api_key.get_secret_value() == "fc-test"
    assert "PUBLIC KEY" in s.clerk_jwt_key
    assert s.app_env in {"dev", "test", "prod"}


def test_settings_missing_required_raises(monkeypatch):
    monkeypatch.delenv("DATABASE_URL", raising=False)
    monkeypatch.delenv("OPENAI_API_KEY", raising=False)
    import pytest
    from pydantic import ValidationError

    with pytest.raises(ValidationError):
        Settings(_env_file=None)
```

并建空 `__init__.py`：

```bash
touch backend/app/__init__.py backend/app/core/__init__.py backend/tests/__init__.py backend/tests/unit/__init__.py
```

- [ ] **Step 2: 运行失败测试**

```bash
cd backend && uv run pytest tests/unit/test_config.py -v
```
Expected: FAIL，`ModuleNotFoundError: app.core.config`。

- [ ] **Step 3: 实现 `app/core/config.py`**

```python
from pydantic import SecretStr
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
        case_sensitive=False,
    )

    app_env: str = "dev"
    app_name: str = "multi-agent-coach"
    log_level: str = "INFO"

    database_url: str
    redis_url: str

    openai_api_key: SecretStr
    openai_model_chat: str = "gpt-4o"
    openai_model_coach: str = "gpt-4o-mini"
    openai_model_embedding: str = "text-embedding-3-small"

    firecrawl_api_key: SecretStr

    clerk_jwt_key: str  # PEM 公钥字符串，从 Clerk Dashboard → JWT Templates 复制
    clerk_issuer: str = ""

    cors_origins: list[str] = ["http://localhost:3000"]


def get_settings() -> Settings:
    s = Settings()
    if not s.clerk_issuer and s.clerk_jwt_key:
        # 从 PEM 中提取 kid 不现实，直接用 clerk domain；D1 写死 dev 域
        pass
    return s
```

- [ ] **Step 4: 写 `backend/.env.example`**

```dotenv
APP_ENV=dev
APP_NAME=multi-agent-coach
LOG_LEVEL=INFO

DATABASE_URL=postgresql+asyncpg://coach:coach@localhost:5432/coach
REDIS_URL=redis://localhost:6379/0

OPENAI_API_KEY=sk-replace-me
OPENAI_MODEL_CHAT=gpt-4o
OPENAI_MODEL_COACH=gpt-4o-mini
OPENAI_MODEL_EMBEDDING=text-embedding-3-small

FIRECRAWL_API_KEY=fc-replace-me

# Clerk（从 Dashboard → Developers → API Keys 拿）
CLERK_PUBLISHABLE_KEY=pk_test_replacethis  # 仅前端用，不写入 backend/.env（此文件放这里备忘）
CLERK_SECRET_KEY=sk_test_replacethis       # 后端用，务必保密
CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----
从 Dashboard → JWT Templates → Copy public key 粘贴到这里
-----END PUBLIC KEY-----"
CLERK_ISSUER=https://clerk.yourapp.accounts.dev  # 从 Dashboard → JWT Templates 看

CORS_ORIGINS=["http://localhost:3000"]
```

- [ ] **Step 5: 跑测试通过**

```bash
cd backend && uv run pytest tests/unit/test_config.py -v
```
Expected: 2 passed。

- [ ] **Step 6: commit**

```bash
git add backend/app/__init__.py backend/app/core/__init__.py backend/app/core/config.py backend/.env.example backend/tests/__init__.py backend/tests/unit/__init__.py backend/tests/unit/test_config.py
git commit -m "feat(config): add pydantic-settings with env validation"
```

---

## Task 5: backend 日志层（structlog）

**Files:**
- Create: `backend/app/core/logging.py`
- Test: `backend/tests/unit/test_logging.py`

- [ ] **Step 1: 写失败测试**

```python
import structlog

from app.core.logging import configure_logging, get_logger


def test_get_logger_returns_bound_logger():
    configure_logging(level="INFO")
    log = get_logger("test")
    assert log is not None
    assert isinstance(log, structlog.stdlib.BoundLogger) or hasattr(log, "info")


def test_structured_log_emits_json(capsys):
    configure_logging(level="INFO", json_output=True)
    log = get_logger("test")
    log.info("hello", foo="bar")
    captured = capsys.readouterr()
    assert "hello" in captured.out
    assert "foo" in captured.out
```

- [ ] **Step 2: 跑失败测试**

```bash
cd backend && uv run pytest tests/unit/test_logging.py -v
```
Expected: FAIL `ModuleNotFoundError: app.core.logging`。

- [ ] **Step 3: 实现 `app/core/logging.py`**

```python
import logging
import sys

import structlog


def configure_logging(level: str = "INFO", json_output: bool = False) -> None:
    log_level = getattr(logging, level.upper(), logging.INFO)

    timestamper = structlog.processors.TimeStamper(fmt="iso")
    shared_processors = [
        structlog.contextvars.merge_contextvars,
        structlog.processors.add_log_level,
        timestamper,
        structlog.processors.StackInfoRenderer(),
    ]

    renderer = (
        structlog.processors.JSONRenderer()
        if json_output
        else structlog.dev.ConsoleRenderer(colors=True)
    )

    structlog.configure(
        processors=[
            *shared_processors,
            structlog.processors.format_exc_info,
            renderer,
        ],
        wrapper_class=structlog.make_filtering_bound_logger(log_level),
        context_class=dict,
        logger_factory=structlog.PrintLoggerFactory(file=sys.stdout),
        cache_logger_on_first_use=True,
    )


def get_logger(name: str | None = None):
    return structlog.get_logger(name)
```

- [ ] **Step 4: 跑测试通过**

```bash
cd backend && uv run pytest tests/unit/test_logging.py -v
```
Expected: 2 passed。

- [ ] **Step 5: commit**

```bash
git add backend/app/core/logging.py backend/tests/unit/test_logging.py
git commit -m "feat(logging): add structlog configuration"
```

---

## Task 6: Docker Compose（postgres-pgvector + redis）

**Files:**
- Create: `docker-compose.yml`
- Create: `docker/postgres/init.sql`

- [ ] **Step 1: 写 `docker-compose.yml`**

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: coach-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: coach
      POSTGRES_PASSWORD: coach
      POSTGRES_DB: coach
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U coach -d coach"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: coach-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
  redis-data:
```

- [ ] **Step 2: 写 `docker/postgres/init.sql`**

```sql
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

- [ ] **Step 3: 启动 + 健康检查**

```bash
docker compose up -d postgres redis
sleep 5
docker compose ps
```
Expected: 两个 service `healthy`。

- [ ] **Step 4: 验证 pgvector 装好**

```bash
docker compose exec postgres psql -U coach -d coach -c "SELECT extname FROM pg_extension WHERE extname IN ('vector','uuid-ossp','pgcrypto');"
```
Expected: 三行 `vector / uuid-ossp / pgcrypto`。

- [ ] **Step 5: commit**

```bash
git add docker-compose.yml docker/postgres/init.sql
git commit -m "build: add docker compose with pgvector postgres and redis"
```

---

## Task 7: Alembic 初始化 + env 配置（async）

**Files:**
- Create: `backend/alembic.ini`
- Create: `backend/alembic/env.py`
- Create: `backend/alembic/script.py.mako`
- Create: `backend/app/db/__init__.py`
- Create: `backend/app/db/base.py`
- Create: `backend/app/db/session.py`

- [ ] **Step 1: 写 `backend/app/db/base.py`（declarative base）**

```python
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass
```

- [ ] **Step 2: 写 `backend/app/db/session.py`**

```python
from collections.abc import AsyncGenerator

from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine

from app.core.config import get_settings

settings = get_settings()

engine = create_async_engine(
    settings.database_url,
    echo=False,
    pool_pre_ping=True,
    pool_size=5,
    max_overflow=10,
)

async_session_factory = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False,
)


async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        yield session
```

- [ ] **Step 3: 写 `backend/app/db/__init__.py`**

```python
from app.db.base import Base
from app.db.session import async_session_factory, engine, get_db

__all__ = ["Base", "engine", "async_session_factory", "get_db"]
```

- [ ] **Step 4: alembic init**

```bash
cd backend && uv run alembic init -t async alembic
```
Expected: `Creating directory ...alembic` + 生成 `alembic.ini`。

- [ ] **Step 5: 改 `backend/alembic.ini` 关键项**

把生成的 `sqlalchemy.url = ...` 改为 `sqlalchemy.url = ` （留空，由 env.py 注入）。把 `script_location = alembic`。

- [ ] **Step 6: 替换 `backend/alembic/env.py`**

```python
import asyncio
from logging.config import fileConfig

from alembic import context
from sqlalchemy import pool
from sqlalchemy.engine import Connection
from sqlalchemy.ext.asyncio import async_engine_from_config

from app.core.config import get_settings
from app.db.base import Base
from app.models import register_models  # noqa: F401 — ensure models are imported

config = context.config

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

settings = get_settings()
config.set_main_option("sqlalchemy.url", settings.database_url)

target_metadata = Base.metadata


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection: Connection) -> None:
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


def run_migrations_online() -> None:
    asyncio.run(run_async_migrations())


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

- [ ] **Step 7: 占位 `backend/app/models/__init__.py`**

```python
def register_models() -> None:
    """Import all models so Alembic autogenerate sees them."""
    from app.models import core  # noqa: F401
```

并建 `backend/app/models/core.py` 占位（暂空）：

```python
# Models are added in Task 8.
```

- [ ] **Step 8: 验证 alembic 可调用**

```bash
cd backend && cp .env.example .env
# 编辑 .env 把 OPENAI_API_KEY/FIRECRAWL_API_KEY 填本地有效值
cd backend && uv run alembic current
```
Expected: 无报错，输出空（无 head）。

- [ ] **Step 9: commit**

```bash
git add backend/alembic.ini backend/alembic/env.py backend/alembic/script.py.mako backend/alembic/README backend/app/db/__init__.py backend/app/db/base.py backend/app/db/session.py backend/app/models/__init__.py backend/app/models/core.py
git commit -m "build(alembic): init async migration scaffolding"
```

---

## Task 8: SQLAlchemy 模型（7 张表）

**Files:**
- Modify: `backend/app/models/core.py`
- Test: `backend/tests/unit/test_models.py`

- [ ] **Step 1: 写失败测试 `tests/unit/test_models.py`**

```python
from app.db.base import Base


def test_seven_tables_registered():
    table_names = set(Base.metadata.tables.keys())
    expected = {
        "users",
        "rag_chunks",
        "user_profile",
        "interview_session",
        "interview_message",
        "star_stories",
        "weakness_tags",
    }
    assert expected.issubset(table_names), f"missing: {expected - table_names}"


def test_rag_chunks_has_vector_column():
    from app.models.core import RagChunk
    cols = {c.name for c in RagChunk.__table__.columns}
    assert "embedding" in cols
    assert "metadata_json" in cols  # 注意 SQLAlchemy 保留字 metadata


def test_user_profile_has_history_jsonb():
    from app.models.core import UserProfile
    cols = {c.name: c.type.__class__.__name__ for c in UserProfile.__table__.columns}
    assert "history" in cols
    assert "profile_embedding" in cols
```

确保 `backend/app/models/__init__.py` 注册：

```python
def register_models() -> None:
    from app.models import core  # noqa: F401
```

- [ ] **Step 2: 跑失败测试**

```bash
cd backend && uv run pytest tests/unit/test_models.py -v
```
Expected: FAIL `ImportError: cannot import name 'RagChunk' from 'app.models.core'`。

- [ ] **Step 3: 实现 `backend/app/models/core.py`**

```python
import uuid
from datetime import datetime

from pgvector.sqlalchemy import Vector
from sqlalchemy import (
    JSON,
    UUID,
    DateTime,
    Float,
    ForeignKey,
    Integer,
    String,
    Text,
)
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.sql import func

from app.db.base import Base

# Clerk user_id 格式如 "user_2abcDEFghijKLMNOP"（VARCHAR，非整数）


class User(Base):
    __tablename__ = "users"
    id: Mapped[str] = mapped_column(String(64), primary_key=True)  # Clerk user_id
    email: Mapped[str] = mapped_column(String(255), unique=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )


class RagChunk(Base):
    __tablename__ = "rag_chunks"
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, server_default=func.gen_random_uuid()
    )
    source: Mapped[str] = mapped_column(String(100))
    title: Mapped[str | None] = mapped_column(Text, nullable=True)
    content: Mapped[str] = mapped_column(Text)
    embedding: Mapped[list[float] | None] = mapped_column(Vector(1536), nullable=True)
    metadata_json: Mapped[dict] = mapped_column("metadata", JSONB, default=dict)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )


class UserProfile(Base):
    __tablename__ = "user_profile"
    user_id: Mapped[str] = mapped_column(
        String(64), ForeignKey("users.id"), primary_key=True
    )
    goals: Mapped[str | None] = mapped_column(Text, nullable=True)
    experience_summary: Mapped[str | None] = mapped_column(Text, nullable=True)
    tech_strengths: Mapped[dict] = mapped_column(JSONB, default=dict)
    tech_weaknesses: Mapped[dict] = mapped_column(JSONB, default=dict)
    soft_strengths: Mapped[dict] = mapped_column(JSONB, default=dict)
    star_completeness_score: Mapped[float] = mapped_column(Float, default=0.0)
    total_interviews: Mapped[int] = mapped_column(Integer, default=0)
    history: Mapped[list] = mapped_column(JSONB, default=list)
    profile_embedding: Mapped[list[float] | None] = mapped_column(
        Vector(1536), nullable=True
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )


class InterviewSession(Base):
    __tablename__ = "interview_session"
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, server_default=func.gen_random_uuid()
    )
    user_id: Mapped[str] = mapped_column(String(64), ForeignKey("users.id"))
    started_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    ended_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    summary: Mapped[str | None] = mapped_column(Text, nullable=True)
    scores: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
    phase_completed: Mapped[dict | None] = mapped_column(JSONB, nullable=True)


class InterviewMessage(Base):
    __tablename__ = "interview_message"
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, server_default=func.gen_random_uuid()
    )
    session_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("interview_session.id")
    )
    role: Mapped[str] = mapped_column(String(20))
    content: Mapped[str] = mapped_column(Text)
    retrieved_context: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
    reflexion_payload: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )


class StarStory(Base):
    __tablename__ = "star_stories"
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, server_default=func.gen_random_uuid()
    )
    user_id: Mapped[str] = mapped_column(String(64), ForeignKey("users.id"))
    project_name: Mapped[str | None] = mapped_column(String(255), nullable=True)
    situation: Mapped[str | None] = mapped_column(Text, nullable=True)
    task: Mapped[str | None] = mapped_column(Text, nullable=True)
    action: Mapped[str | None] = mapped_column(Text, nullable=True)
    result: Mapped[str | None] = mapped_column(Text, nullable=True)
    quantified_results: Mapped[str | None] = mapped_column(Text, nullable=True)
    tech_stack: Mapped[list | None] = mapped_column(JSONB, nullable=True)
    quality_score: Mapped[float | None] = mapped_column(Float, nullable=True)
    source_message_id: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True), ForeignKey("interview_message.id"), nullable=True
    )
    embedding: Mapped[list[float] | None] = mapped_column(Vector(1536), nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )


class WeaknessTag(Base):
    __tablename__ = "weakness_tags"
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, server_default=func.gen_random_uuid()
    )
    user_id: Mapped[str] = mapped_column(String(64), ForeignKey("users.id"))
    tag: Mapped[str] = mapped_column(String(100))
    category: Mapped[str] = mapped_column(String(50))
    severity: Mapped[float] = mapped_column(Float)
    occurrence_count: Mapped[int] = mapped_column(Integer, default=1)
    last_occurred_session: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True), nullable=True
    )
    related_message_ids: Mapped[list | None] = mapped_column(JSONB, nullable=True)
    status: Mapped[str] = mapped_column(String(20), default="active")
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )
```

- [ ] **Step 4: 跑测试通过**

```bash
cd backend && uv run pytest tests/unit/test_models.py -v
```
Expected: 3 passed。

- [ ] **Step 5: commit**

```bash
git add backend/app/models/core.py backend/tests/unit/test_models.py
git commit -m "feat(models): add 7 SQLAlchemy models for M2 schema"
```

---

## Task 9: 首次 Alembic 自动生成迁移 + 应用

**Files:**
- Auto-create: `backend/alembic/versions/<hash>_init_schema.py`

- [ ] **Step 1: 自动生成迁移**

```bash
cd backend && uv run alembic revision --autogenerate -m "init schema"
```
Expected: 输出 `Generating ...versions/xxxx_init_schema.py`。

- [ ] **Step 2: 人工检查生成的迁移**

打开新生成的迁移文件，确认：
- 包含 `op.create_table("users", ...)` 等 7 张表
- pgvector 字段 `embedding`/`profile_embedding` 类型是 `Vector(1536)`（如果 autogenerate 未识别 Vector，需要 import 并手工补 `op.execute("CREATE EXTENSION IF NOT EXISTS vector")`，并在迁移顶部加 `import pgvector.sqlalchemy`）

如果迁移文件顶部缺少 `import pgvector.sqlalchemy`，手工加上：

```python
import pgvector.sqlalchemy
```

- [ ] **Step 3: 应用迁移**

```bash
cd backend && uv run alembic upgrade head
```
Expected: `Running upgrade  -> <hash>, init schema`。

- [ ] **Step 4: 验证 7 张表都建好**

```bash
docker compose exec postgres psql -U coach -d coach -c "\dt"
```
Expected: 列出 8 张表（含 `alembic_version` + 7 业务表）。

- [ ] **Step 5: 验证 pgvector 列**

```bash
docker compose exec postgres psql -U coach -d coach -c "\d rag_chunks"
```
Expected: 看到 `embedding | vector(1536)`。

- [ ] **Step 6: 跑一次降级 + 升级，确认可重放**

```bash
cd backend && uv run alembic downgrade base && uv run alembic upgrade head
```
Expected: 无错。

- [ ] **Step 7: commit**

```bash
git add backend/alembic/versions/*.py
git commit -m "feat(db): init schema migration (7 tables + pgvector)"
```

---

## Task 10: 数据层集成测试（CRUD smoke）

**Files:**
- Test: `backend/tests/integration/test_db_crud.py`
- Create: `backend/tests/integration/__init__.py`
- Create: `backend/tests/conftest.py`

- [ ] **Step 1: 写 `tests/conftest.py`**

```python
import asyncio
from collections.abc import AsyncGenerator

import pytest
import pytest_asyncio
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import async_session_factory


@pytest.fixture(scope="session")
def event_loop():
    loop = asyncio.new_event_loop()
    yield loop
    loop.close()


@pytest_asyncio.fixture
async def db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        yield session
        await session.rollback()
```

- [ ] **Step 2: 写失败测试 `tests/integration/test_db_crud.py`**

```python
import pytest
from sqlalchemy import select

from app.models.core import User


@pytest.mark.asyncio
async def test_create_and_select_user(db):
    u = User(id="user_test01abc", email="test+d1@example.com")
    db.add(u)
    await db.flush()
    assert u.id == "user_test01abc"
    result = await db.execute(select(User).where(User.email == "test+d1@example.com"))
    fetched = result.scalar_one()
    assert fetched.id == "user_test01abc"
```

- [ ] **Step 3: 跑测试**

```bash
cd backend && uv run pytest tests/integration/test_db_crud.py -v
```
Expected: 1 passed（依赖 docker compose 已起 + alembic upgrade 已跑）。

- [ ] **Step 4: commit**

```bash
git add backend/tests/conftest.py backend/tests/integration/__init__.py backend/tests/integration/test_db_crud.py
git commit -m "test(db): add CRUD smoke test for User model"
```

---

## Task 11: FastAPI 主入口 + /health 端点

**Files:**
- Create: `backend/app/main.py`
- Create: `backend/app/api/__init__.py`
- Create: `backend/app/api/v1/__init__.py`
- Create: `backend/app/api/v1/health.py`
- Test: `backend/tests/integration/test_health.py`

- [ ] **Step 1: 写失败测试**

```python
import pytest
from httpx import ASGITransport, AsyncClient


@pytest.mark.asyncio
async def test_health_returns_ok():
    from app.main import app

    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        r = await ac.get("/api/v1/health")
    assert r.status_code == 200
    data = r.json()
    assert data["status"] == "ok"
    assert "db" in data
    assert "redis" in data
```

- [ ] **Step 2: 跑失败测试**

```bash
cd backend && uv run pytest tests/integration/test_health.py -v
```
Expected: FAIL `ModuleNotFoundError: app.main`。

- [ ] **Step 3: 实现 `app/api/v1/health.py`**

```python
from fastapi import APIRouter, Depends
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_db

router = APIRouter()


@router.get("/health")
async def health(db: AsyncSession = Depends(get_db)):
    db_ok = False
    try:
        await db.execute(text("SELECT 1"))
        db_ok = True
    except Exception:
        db_ok = False

    redis_ok = False
    try:
        from redis.asyncio import Redis
        from app.core.config import get_settings
        r = Redis.from_url(get_settings().redis_url)
        pong = await r.ping()
        await r.close()
        redis_ok = bool(pong)
    except Exception:
        redis_ok = False

    return {
        "status": "ok" if (db_ok and redis_ok) else "degraded",
        "db": db_ok,
        "redis": redis_ok,
    }
```

- [ ] **Step 4: 实现 `app/main.py`**

```python
from contextlib import asynccontextmanager

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.api.v1 import health as health_v1
from app.core.config import get_settings
from app.core.logging import configure_logging, get_logger

settings = get_settings()
configure_logging(level=settings.log_level, json_output=(settings.app_env != "dev"))
log = get_logger("app.main")


@asynccontextmanager
async def lifespan(app: FastAPI):
    log.info("startup", app_env=settings.app_env)
    yield
    log.info("shutdown")


app = FastAPI(title=settings.app_name, version="0.1.0", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.exception_handler(Exception)
async def global_exception_handler(request, exc):
    from fastapi.responses import JSONResponse
    log.exception("unhandled_exception", path=str(request.url), error=str(exc))
    return JSONResponse(status_code=500, content={"detail": "internal_server_error"})


app.include_router(health_v1.router, prefix="/api/v1", tags=["health"])
```

- [ ] **Step 5: 加 init 文件**

```bash
touch backend/app/api/__init__.py backend/app/api/v1/__init__.py
```

- [ ] **Step 6: 跑测试通过**

```bash
cd backend && uv run pytest tests/integration/test_health.py -v
```
Expected: 1 passed。

- [ ] **Step 7: 手动起服务验证**

```bash
cd backend && uv run uvicorn app.main:app --port 8000 &
sleep 2
curl -s http://localhost:8000/api/v1/health | python -m json.tool
kill %1
```
Expected: JSON 输出 `"status": "ok", "db": true, "redis": true`。

- [ ] **Step 8: commit**

```bash
git add backend/app/main.py backend/app/api/__init__.py backend/app/api/v1/__init__.py backend/app/api/v1/health.py backend/tests/integration/test_health.py
git commit -m "feat(api): add FastAPI app with /health endpoint"
```

---

## Task 12: Clerk 后端 Auth 占位文件

**Files:**
- Create: `backend/app/core/auth.py`
- Create: `clerk-public.pem`（根目录占位）
- Test: `backend/tests/unit/test_auth.py`

- [ ] **Step 1: 写失败测试（验证 JWT 校验骨架）**

```python
def test_get_current_user_id_rejects_no_header():
    import pytest
    from fastapi import HTTPException
    from app.core.auth import get_current_user_id

    async def run():
        with pytest.raises(HTTPException) as exc:
            await get_current_user_id(authorization="")
        assert exc.value.status_code == 401
```

- [ ] **Step 2: 跑失败测试**

```bash
cd backend && uv run pytest tests/unit/test_auth.py -v
```
Expected: FAIL。

- [ ] **Step 3: 实现 `app/core/auth.py`**

```python
import jwt
from fastapi import Header, HTTPException

from app.core.config import get_settings

_settings = get_settings()

# D1: 从 Clerk Dashboard → JWT Templates 复制 PEM 公钥到根目录 clerk-public.pem
# 生产环境改为读 CLERK_JWT_KEY 环境变量
try:
    with open("clerk-public.pem") as f:
        _pem = f.read()
except FileNotFoundError:
    _pem = _settings.clerk_jwt_key or ""


async def get_current_user_id(authorization: str = Header(..., description="Bearer <clerk_jwt>")) -> str:
    if not authorization or not authorization.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="missing or invalid authorization header")
    token = authorization.removeprefix("Bearer ").strip()
    if not token:
        raise HTTPException(status_code=401, detail="empty token")
    try:
        payload = jwt.decode(
            token,
            _pem,
            algorithms=["RS256"],
            options={"verify_aud": False, "verify_iat": True},
        )
        sub = payload.get("sub")
        if not sub:
            raise HTTPException(status_code=401, detail="token missing sub claim")
        return sub
    except jwt.PyJWTError as exc:
        raise HTTPException(status_code=401, detail=f"invalid token: {exc}")


async def get_optional_user_id(
    authorization: str | None = Header(None, description="Optional Bearer <clerk_jwt>"),
) -> str | None:
    """用于 health endpoint 等不需要鉴权的场景。"""
    if not authorization:
        return None
    try:
        return await get_current_user_id(authorization=authorization)
    except HTTPException:
        return None
```

- [ ] **Step 4: 建占位 PEM 文件**

```bash
echo "Placeholder - replace with real Clerk PEM from Dashboard" > /Users/xuebao/learn/AI项目/multi-agent-coach/clerk-public.pem
```

- [ ] **Step 5: 跑测试通过**

```bash
cd backend && uv run pytest tests/unit/test_auth.py -v
```
Expected: 1 passed（验证空 header 返回 401）。

- [ ] **Step 6: commit**

```bash
git add backend/app/core/auth.py backend/tests/unit/test_auth.py clerk-public.pem
git commit -m "feat(auth): add Clerk JWT verification middleware (stub PEM)"
```

---

## Task 13: 前端 Next.js 16 初始化 + Clerk

**Files:**
- Auto-create: `frontend/*`（pnpm create）
- Create: `frontend/.env.local`（Clerk 前端 keys）
- Create: `frontend/middleware.ts`（Clerk route protection）

- [ ] **Step 1: 用 pnpm create 初始化 Next.js**

```bash
cd /Users/xuebao/learn/AI项目/multi-agent-coach
rm -rf frontend  # 删占位
pnpm create next-app@latest frontend --typescript --tailwind --app --src-dir=false --import-alias "@/*" --use-pnpm --no-eslint --turbopack
```
Expected: 交互完成，生成 `frontend/` 完整项目。

- [ ] **Step 2: 装 Clerk SDK**

```bash
cd frontend && pnpm add @clerk/nextjs
```
Expected: `dependencies: + @clerk/nextjs`。

- [ ] **Step 3: 初始化 shadcn/ui**

```bash
cd frontend && pnpm dlx shadcn@latest init -y -d
```
Expected: 生成 `components.json`，`lib/utils.ts`。

- [ ] **Step 4: 装常用 shadcn 组件（D6 会用到）**

```bash
cd frontend && pnpm dlx shadcn@latest add button card input scroll-area separator sheet textarea chart
```

- [ ] **Step 5: 写 `frontend/middleware.ts`（Clerk 路由保护）**

```typescript
import { clerkMiddleware } from "@clerk/nextjs/server";

export default clerkMiddleware();

export const config = {
  matcher: [
    "/((?!_next|.*\\..*|api/backend).*)", // 保护所有页面，但不拦截后端 API 代理
  ],
};
```

- [ ] **Step 6: 写 `frontend/.env.local`**

```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replacethis
CLERK_SECRET_KEY=sk_test_replacethis
```

- [ ] **Step 7: 改 `frontend/next.config.ts` 加后端代理**

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  async rewrites() {
    return [
      {
        source: "/api/backend/:path*",
        destination: "http://localhost:8000/api/:path*",
      },
    ];
  },
};

export default nextConfig;
```

- [ ] **Step 8: 起服务验证**

```bash
cd frontend && pnpm dev &
sleep 5
curl -s http://localhost:3000 | head -5
# 应该被 Clerk middleware 重定向到 /sign-in
kill %1
```
Expected: HTML 输出包含重定向或 Clerk script。

- [ ] **Step 9: commit**

```bash
git add frontend/ .gitignore
# 确保 frontend/.env.local 在 .gitignore 中
git commit -m "feat(frontend): init next.js 16 + clerk + tailwind + shadcn"
```

> ⚠️ D6 之前 Clerk 路由保护会让所有页面跳转到登录页，这意味着 D3 前端简陋 UI 测试需要临时跳过 middleware（把 `matcher` 注释掉一行）。同 D6 Task 3 正式 unlock。


---

## Task 14: `dev.sh` 一键启动脚本

**Files:**
- Create: `dev.sh`
- Modify: 文件权限 +x

- [ ] **Step 1: 写 `dev.sh`**

```bash
#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT_DIR"

echo "==> Starting docker compose (postgres + redis)"
docker compose up -d postgres redis

echo "==> Waiting for postgres healthy"
for i in {1..30}; do
  if docker compose ps postgres | grep -q "healthy"; then
    echo "    postgres ready"
    break
  fi
  sleep 1
done

echo "==> Applying alembic migrations"
(cd backend && uv run alembic upgrade head)

echo "==> Starting backend (uvicorn)"
(cd backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000) &
BACKEND_PID=$!

echo "==> Starting celery worker (deferred to D4)"
# (cd backend && uv run celery -A app.workers.celery_app worker --loglevel=info) &
# CELERY_PID=$!

echo "==> Starting frontend (next dev)"
(cd frontend && pnpm dev) &
FRONTEND_PID=$!

cleanup() {
  echo "==> Shutting down"
  kill $BACKEND_PID 2>/dev/null || true
  kill $FRONTEND_PID 2>/dev/null || true
  wait
}
trap cleanup INT TERM

echo ""
echo "🚀 Services running:"
echo "   Backend:  http://localhost:8000/api/v1/health"
echo "   Frontend: http://localhost:3000"
echo "   Press Ctrl+C to stop"
echo ""

wait
```

- [ ] **Step 2: chmod +x**

```bash
chmod +x dev.sh
```

- [ ] **Step 3: 端到端验证**

```bash
./dev.sh &
sleep 10
curl -s http://localhost:8000/api/v1/health | python -m json.tool
curl -sI http://localhost:3000 | head -1
# 看到 backend health ok + frontend 200 OK 后：
kill %1
docker compose down
```
Expected: backend 返回 `{"status":"ok","db":true,"redis":true}`，frontend 返回 `HTTP/1.1 200 OK`。

- [ ] **Step 4: commit**

```bash
git add dev.sh
git commit -m "build: add dev.sh one-shot startup script"
```

---

## Task 15: D1 EOD 验收 + 总结 commit

- [ ] **Step 1: 跑全部测试**

```bash
cd backend && uv run pytest -v
```
Expected: 全部 passed（config 2 + logging 2 + models 3 + auth 1 + db CRUD 1 + health 1 = 10 个）。

- [ ] **Step 2: 验收清单核对**

D1 验收（参考 `docs/superpowers/plans/2026-05-19-00-index.md` §4）：

- [ ] 仓库初始化 + `.gitignore` 覆盖完整
- [ ] `backend/` 用 uv 管理，Python 3.12，`uv sync` 成功
- [ ] `frontend/` 用 pnpm，Next.js 16 + Tailwind + shadcn 起来
- [ ] `docker-compose.yml` 含 postgres-pgvector + redis，`up -d` 后 healthy
- [ ] `dev.sh` 一键起服务无错
- [ ] Alembic 迁移生成 + 应用 + 可降级再升级
- [ ] 7 张表都建立（含 pgvector 列）
- [ ] FastAPI `/api/v1/health` 返回 `{"status":"ok"}`
- [ ] pydantic-settings 校验 env，缺 key 时正确报错
- [ ] structlog 配置好
- [ ] `clerk-public.pem` 占位文件存在
- [ ] Clerk `@clerk/nextjs` SDK 已装到前端
- [ ] `backend/app/core/auth.py` JWT 校验 middleware 存在并能拒绝空 token（测试验证）
- [ ] `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` / `CLERK_SECRET_KEY` 在 `frontend/.env.local` 配置好
- [ ] 10 个测试全绿

- [ ] **Step 3: 推到 GitHub**

```bash
git remote add origin git@github.com:<your-handle>/multi-agent-coach.git
git branch -M main
git push -u origin main
```
Expected: GitHub 公开仓库可访问。

- [ ] **Step 4: 标记 D1 完成 commit（empty merge 标记）**

```bash
git commit --allow-empty -m "feat: D1 EOD - 工程脚手架 + 数据层 ✅"
git push
```

---

## 风险与延期预案（D1 专用）

| 风险 | 预案 |
|---|---|
| pgvector 扩展安装失败 | 必须用 `pgvector/pgvector:pg16` 镜像，不要用纯 `postgres:16` 自己装 | 
| `uv sync` 装包慢 / 失败 | 用 `UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple` 加速 |
| Alembic autogenerate 未识别 Vector | 手工补 `import pgvector.sqlalchemy` 到迁移文件顶部 |
| Next.js 16 create 报错（版本太新） | 锁版本：`pnpm create next-app@16.0.0 ...` |
| shadcn init 询问交互卡住 | 用 `-y -d` 参数（已加） |
| Docker 端口 5432/6379 已占用 | 改 `docker-compose.yml` 端口映射为 `15432:5432` / `16379:6379`，同步改 `.env` |

如果 D1 EOD 仍未完成 Task 10-13 中的任一项，**不允许**进入 D2，必须延 D1 半天补完——D2 强依赖数据层稳定。

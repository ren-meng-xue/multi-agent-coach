# Multi Agent Coach · Agent 规范入口

> 本仓库的工程规范统一维护在 [`CLAUDE.md`](./CLAUDE.md)，单一来源，避免多份文档不同步。

**在开始任何工作前，必须先完整阅读同目录下的 `CLAUDE.md`，并严格遵守其中全部条款：**

- Git 规则
- 开发流程
- 代码设计原则
- 后端规范
- 前端规范
- 注释规范
- Review 重点
- 测试规范
- 验证命令
- 默认工作流

本文件供读取 `AGENTS.md` 的工具（如 Codex CLI）使用；Claude Code 直接读取 `CLAUDE.md`。两者共用同一份规范，请勿在此重复维护规范正文——如需修改规范，只改 `CLAUDE.md`。
